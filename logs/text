- truy vấn vào container khi docker-compose up
    docker-compose exec "tên service" commandline
    example:
        docker-compose exec server npm restart
        docker-compose exec postgres psql -U root express
        docker-compose exec postgres sh


- start postgres
    sudo -u postgres psql

- commandline rabbit
    + login to container rabbit: docker-compose exec rabbitmq sh
    + get list queues: rabbitmqctl list_queues

- stop rabbit: sudo systemctl stop rabbitmq-server
- stop elas: sudo systemctl stop elasticsearch.service
